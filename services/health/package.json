{
  "name": "@returnshield/health-service",
  "version": "0.1.0",
  "private": true,
  "description": "GET /v1/health — liveness endpoint using the frozen response envelope.",
  "type": "module",
  "main": "./src/handler.ts",
  "scripts": {
    "dev": "tsx watch src/local-server.ts",
    "start": "tsx src/local-server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@returnshield/contracts": "0.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.145",
    "tsx": "4.19.1",
    "typescript": "5.6.3",
    "vitest": "2.1.3"
  }
}
