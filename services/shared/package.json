{
  "name": "@returnshield/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-service shared runtime modules. Phase 02 (task P2-IDEM-01) adds the idempotency subsystem under src/idempotency.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.687.0",
    "@aws-sdk/lib-dynamodb": "3.687.0"
  },
  "devDependencies": {
    "@types/node": "20.16.11",
    "typescript": "5.6.3",
    "vitest": "2.1.3"
  }
}
