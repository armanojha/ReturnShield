# Deterministic dependency policy.
# - one package manager (npm) and one lockfile (package-lock.json) at the root
# - exact versions only; no floating ranges are introduced by `npm install`
# - CI installs with `npm ci`, which fails if the lockfile is out of date
save-exact=true
engine-strict=false
fund=false
audit=false
